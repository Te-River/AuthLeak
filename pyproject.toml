[project]
name = "AuthLeak"
version = "1.0.0"
description = "自动获取更新包并解密"
requires-python = ">=3.9"
dependencies = [
    "pycryptodome>=3.20.0",
    "requests>=2.31.0",
    "loguru>=0.7.2",
]

[tool.uv]
[project]
name = "AuthLeak"
version = "1.0.0"
description = "自动获取更新包并解密"
requires-python = ">=3.10"
dependencies = [
    "pycryptodome>=3.20.0",
    "requests>=2.31.0",
    "loguru>=0.7.2",
    "cryptocode>=0.1",
    "tqdm>=4.66.0",
    "limiter>=0.5.0",
]

[tool.uv]